#include <stdio.h>
#include <math.h>

int main()
{
    int a = 22;
    int b = a;
    int c = b * c;
    int d = 1, e;

    int oldAge = 22;
    int years = 2;
    int newAge = oldAge + years;

    int x,y,z = 4;
    x = y = z = 4;

    int a = 1, b = 2;
    int sum = a + b;
    int multiply = a * b;
    int x, y = a * b;

    int b, c;
    b = c = 1;
    int a = b + c;
    int power = pow(b, c);
    printf("%d", power);

    printf("%d", 16 % 10);

    return 0;
}
